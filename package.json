{
  "name": "thedevjournal",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "bun --filter '*' dev",
    "dev:api": "bun --cwd apps/api dev",
    "dev:web": "bun --cwd apps/web dev"
  },
  "devDependencies": {
    "vite-plugin-pwa": "^1.3.0"
  }
}
